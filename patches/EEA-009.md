# EEA-009: Upstream Backports & Hotfixes

## Files Modified
Various backend files (e.g., `alembic` revisions, Confluence `handleRequest` fix).

## Intent & Description
Commits cherry-picked from newer Onyx versions before doing a full upgrade to fix urgent bugs.

## Architectural Decisions & Upstream Conflicts
Low. Git should handle exact cherry-picks cleanly.

## Migration & Resolution Strategy
Upon merging `v2.12.1`, git will likely auto-resolve these. If conflicts arise, accept the upstream `v2.12.1` version of the file since it contains the official fix.
